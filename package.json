{
  "name": "ontraport-mcp-server",
  "version": "1.0.0",
  "description": "Ontraport MCP Server — exposes Ontraport functionality as structured tools for MCP-compatible AI agents",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
